# Frontend Tablerillos — variables Vite (prefijo VITE_ para exponer al cliente)

# URL del backend para el proxy de Vite en dev (el SPA usa rutas /api relativas).
VITE_BACKEND_URL=http://localhost:8000
